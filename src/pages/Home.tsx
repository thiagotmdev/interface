import Button from "../components/Button";

const Home = () => {
  return (
    <div>
      <h2>Home</h2>
      <Button variant="outline" isLoading>
        Clique em min
      </Button>
    </div>
  );
};

export default Home;
